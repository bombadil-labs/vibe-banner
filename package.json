{
  "name": "vibe-annotation-renderer",
  "version": "0.0.4",
  "description": "Renders Claude's vibe-annotation mood banner from feeling-values. Loaded by the vibe-annotations skill via jsDelivr; the skill passes only the values.",
  "license": "MIT",
  "private": false,
  "main": "src/vibe.js",
  "scripts": {
    "build": "esbuild src/vibe.js --minify --outfile=dist/vibe.min.js",
    "parity": "node test/parity.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
